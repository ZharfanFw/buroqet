apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: buroqet
  labels:
    app: settlement-service
spec:
  selector:
    app: settlement-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8081
  type: ClusterIP