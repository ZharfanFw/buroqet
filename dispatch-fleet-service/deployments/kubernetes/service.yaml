apiVersion: v1
kind: Service
metadata:
  name: dispatch-fleet-service
spec:
  selector:
    app: dispatch-fleet
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: LoadBalancer # Agar dapat IP eksternal (bisa pakai NodePort jika lokal)