apiVersion: v1
kind: Service
metadata:
  name: dispatch-fleet-service
  namespace: buroqet
  labels:
    app: dispatch-fleet-service
spec:
  selector:
    app: dispatch-fleet-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8081
  type: ClusterIP